# Deployment environment variables template
# Copy to .env and fill secrets before running in production
FLASK_DEBUG=0
PORT=5000
GUNICORN_WORKERS=2
# Example: set to a production secret/store connection string if used
PREDICTION_DB_URL=
SECRET_KEY=change-me